# گزارش بررسی کد — Gold Bubble (v2.1.0)

تاریخ بررسی: ۶ اوت ۲۰۲۶ · بر پایهی کامیت `95ceb91` (v2.0.5) + اصلاحات این جلسه

---

## ۱) خلاصهی وضعیت

| معیار | نتیجه |
|---|---|
| تستها (پس از اصلاح) | ✅ ۷۸ پاس در ~۱ ثانیه (قبلاً: ۱۸۰+ ثانیه قفل) |
| اجرای زنده | ✅ ۸ منبع از ۱۵ زنده: TGJU، ناوَسان، gold-api، CoinGecko، اکسیر |
| دادهی زنده | انس ~۴٬۲۶۴$ · دلار ~۱۸۵٬۰۰۰ ت · طلای ۱۸ ~۱۸٫۶۲M ت/کیلو (از منابع واقعی) |
| نسخه | 2.0.5 ← **2.1.0** |

> صرافیهای ارز دیجیتال ایرانی (نوبیتکس، والکس، بیتپین، تَبْدیل، رمزینکس، تترلند، آبانتتر) از **این** محیط سندباکس بلاک هستند (SSL handshake timeout). روی سرور ایران خودتان اینها هم کار میکنند.

---

## ۲) باگهای پیدا شده و اصلاح شده

### ۲٫۱ تستها به اینترنت واقعی وابسته بودند (قفل ۹۰+ ثانیهای) — اصلاح شد
`_client()` در `tests/test_trade.py` و `tests/test_wallet.py` از `TestClient(app)` استفاده میکند که در **lifespan** یک `refresh_once()` واقعی اجرا میکند. وقتی منبعی بلاک باشد (مثلاً نوبیتکس از خارج ایران)، هر تست تا ~۹۰ ثانیه (بودجهی fan-out) قفل میشود و کل سویت میخوابد — دقیقاً همان اتفاقی که در این بررسی افتاد.
**اصلاح:** فیکسچر `_hermetic_refresh` با یک مدل آماده، `app.refresher.build_model` را در هر دو فایل جایگزین میکند. تستها حالا هیچ وابستگی به اتصال خروجی ندارند: **۷۶ تست در ۰٫۹ ثانیه**.

### ۲٫۲ رشد بیپایان `price_points` — اصلاح شد
جدول تاریخچه هر ۱۵ ثانیه ~۲۰ ردیف مینویسد ≈ **۱۱۵هزار ردیف/روز ≈ ۳٫۵ میلیون/ماه** بدون هیچ پاکسازی. در SQLite فایل و در Postgres دیتابیس بیحساب بزرگ میشود و کوئری `/api/history` به مرور کند میشود.
**اصلاح:** `PRICE_HISTORY_DAYS` (پیشفرض ۱۴ روز) + `cleanup_history()` — یک بار در startup و بعد هر ۱۰۰ چرخه (~۲۵ دقیقه) در حلقهی رفرش. هر بار فقط ردیفهای قدیمیتر از سقف حذف میشوند؛ شکست پاکسازی هیچوقت رفرش را نمیشکند.

### ۲٫۳ حملهی بیوقفه به منابع مرده — اصلاح شد
قبلاً `next_at = max(started + REFRESH_SEC, time.time() + 1.0)` بود: اگر یک چرخه ۹۰ ثانیهای طول میکشید، چرخهی بعدی فقط **۱ ثانیه بعد** شروع میشد — با پروکسی/منبع مرده یعنی حلقهی بیپایان چرخههای ۹۰ ثانیهای پشتسرهم.
**اصلاح:** تابع خالص `next_wait(duration, interval)` — چرخهی عادی همان کادنسبت ثابت را دارد؛ چرخهی اوررانشده به اندازهی اورران (سقف: یک اینتروال) تنفس میگیرد.

### ۲٫۴ اتصالهای کیف پول با پروکسی مرده میمردند — اصلاح شد
v2.0.5 مشکل «پروکسی مرده» را فقط برای providerها حل کرد (`_http_get` → fallback مستقیم). اما `wallet.fetch_balance` همچنان یکطرفه `trust_env=True` بود: با پروکسی خراب، همهی موجودیها فیل میشدند.
**اصلاح:** همان الگو — یک تلاش از طریق پروکسی، در شکست (و فقط برای **GET** چون خواندنی است) یک تلاش مستقیم. **POST هرگز دوباره ارسال نمیشود** (ریسک ثبت دوبارهی سفارش). تست: `test_fetch_balance_falls_back_to_direct_when_proxy_dead`.
> نکته: سفارشهای معاملاتی (trade.send_order) عمداً retry نمیشوند — ارسال دوبارهی POST بعد از timeout اتصال میتواند سفارش را دوبار ثبت کند.

### ۲٫۵ API بدون هیچ احراز هویتی (ریسک مالی) — اصلاح شد (اختیاری)
هر کسی در شبکه میتوانست `POST /api/trade/orders` با `confirm: true` بفرستد و سفارش واقعی ثبت کند، یا اتصالهای کیف پول را دستکاری کند. **اصلاح:** `API_TOKEN` اختیاری — وقتی ست شود، همهی POST/PATCH/DELETE های `/api/wallet/connections*` و `/api/trade/*` باید هدر `X-API-Token` داشته باشند؛ بدون توکن (پیشفرض) رفتار قبلی حفظ میشود. خواندنها (قیمتها، لیستها) همیشه باز میمانند. تست: `test_api_token_guards_order_placement_when_set` و `test_api_token_guards_wallet_writes_when_set`.
> ⚠️ محدودیت: فرانتِ ساختهشده فعلاً توکن نمیفرستد؛ اگر `API_TOKEN` را فعال کنید، دکمههای سفارش در UI خطای ۴۰۱ میگیرند (فعالسازی صرفاً برای استفادهی script/curl یا بعد از پچ فرانت).

### ۲٫۶ محافظ دفاعی گزارش منابع — اصلاح شد
یک بار در اولین چرخهی رفرش، عنصر `None` در گزارش دیده شد (race احتمالی؛ قابل بازتولید نبود). حالا `build_model` نتایج غیردیکت را پیش از ساخت مدل فیلتر میکند تا یک آیتم خراب کل مدل را از بین نبرد.

---

## ۲٫۵) قابلیتهای جدید (v2.1.0)

### توکن API در فرانت (پشتیبانی کامل UI)
- صفحهی **تنظیمات** حالا یک فیلد «توکن امنیتی API» دارد؛ توکن در localStorage مرورگر ذخیره و با **هر درخواست** به صورت `X-API-Token` ارسال میشود (تمام مسیرهای سفارش/آربیتراژ/اتصالها از همین هدر استفاده میکنند).
- اگر سرور `API_TOKEN` نداشته باشد (پیشفرض)، فرستادن هدر اضافی بیضرر است — همهچیز مثل قبل کار میکند.

### لیست سفید دامنه برای سفارشها (SSRF guard)
- `TRADE_ALLOWED_DOMAINS` (کاما-جدا): وقتی ست شود، URL کانکتورهای سفارش/آربیتراژ باید دقیقاً همان هاست یا زیردامنهی یکی از دامنههای مجاز باشد.
- در **دو لایه** اعمال میشود: هنگام ساخت/ویرایش کانکتور (422 فوری) و دوباره داخل `build_request()` — پس یک کانکتوری که قبل از فعالشدن لیست ساخته شده هم نمیتواند به جای دیگری برود.
- تست زنده: درخواست به `169.254.169.254` (متادیتای AWS) → `422 host '169.254.169.254' is not in TRADE_ALLOWED_DOMAINS` ✅
- نامهای شبیه (`evilnobitex.ir` برای `nobitex.ir`) رد میشوند (مقایسه با مرز نقطه).

---

## ۳) چیزهایی که درست طراحی شدهاند (تأیید شد)

- **merge با آخرین مقدار معتبر:** شکست یک منبع، مقدار قبلی را نگه میدارد — تابلو «فریز» نمیشود.
- **ماسک کردن اسرار:** هدرهای Authorization هرگز از سرور به مرورگر برنمیگردند (`••••••`)، و «فرستادن ماسک به عقب» یعنی «نگهدار».
- **dry-run پیشفرض برای کانکتورهای سفارش:** هیچ کانکتوری قبل از نگاهکردنِ کاربر به درخواستِ پیشنمایش، نمیتواند سفارش واقعی بفرستد.
- **`map_abantether_ticker`:** تشخیص درست کوتیشن dealer (buy_price = چیزی که مشتری میپردازد) — کتاب وارونهی قبلی که آربیتراژ رایگان جعلی میساخت، درست شده.
- **بودجهی fan-out و اسکیپ:** منابع باقیمانده «skipped» گزارش میشوند و مقدار قبلیشان میماند.
- **فرض «ریال» در TGJU درست است:** مقدار خام ۱٬۸۹۰٬۰۰۰ ریال ÷۱۰ = ۱۸۹٬۰۰۰ تومان ✓ (مقایسهی زنده با ناوَسان ~۲٫۴٪ اختلاف طبیعی بین دو منبع).
- **پارتیشنبندی درست متغیرها:** `config` همهچیز را در یکجا میخواند؛ `version.py` کد را مرجع نسخه میگیرد نه env (باگ قبلی «نسخهی قدیمی بعد از دیپلوی»).

---

## ۴) ریسکهای باقیمانده (توصیه برای آینده — اصلاح نشده)

| ریسک | توضیح | پیشنهاد |
|---|---|---|
| **بدون rate-limit** | `/api/wallet/balances` با ThreadPool به endpoint دلخواه کاربر همرینگ میکند | در صورت نیاز، محدودیت ساده |
| **دوبار ثبت سفارش انسانی** | آربیتراژ «خرید→فروش» در یک درخواست؛ اگر پاسخ leg اول گم شود، کاربر ممکن است دوباره بزند | در UI وضعیت leg را از تاریخچه (`/api/trade/orders`) بازیابی کنید |
| **`PORT` در config کد مرده است** | `config.PORT` خوانده میشود ولی استفاده نمیشود (uvicorn از CLI میگیرد) | حذف یا استفاده در Dockerfile |
| **نسخه در compose** | پیشفرض `APP_VERSION: 1.1.1` در docker-compose با کد 2.1.0 متفاوت است (بیضرر چون version.py برنده است) | همگامسازی برای شفافیت |
| **باندل فرانت باید بیلد شود** | تغییرات `frontend/src/` تا وقتی `npm run build` و کپی به `public/` نشود به کاربر نمیرسد | اسکریپت `build` در CI که public/ را از frontend/ بسازد |
| **کیف پول بدون لیست سفید** | allowlist فقط برای کانکتورهای **سفارش** اعمال شد؛ اتصالهای کیف پول (خواندنی) همچنان بازند | در صورت نیاز، `WALLET_ALLOWED_DOMAINS` مشابه |

---

## ۵) فایلهای تغییر یافته

### کامیت اول (`4a0ee00` — v2.0.6)
```
app/config.py      + PRICE_HISTORY_DAYS, API_TOKEN
app/db.py          + cleanup_history()
app/refresher.py   + next_wait(), retention هر ۱۰۰ چرخه
app/providers.py   + فیلتر دفاعی نتایج fan-out
app/wallet.py      + _proxy_configured(), _call_once(), fallback مستقیم برای GET
app/main.py        + _require_token() روی ۱۰ endpoint تغییردهنده، پاکسازی startup
app/version.py     2.0.5 ← 2.0.6
tests/test_maintenance.py   (جدید) ۶ تست
tests/test_trade.py         فیکسچر بدون-اینترنت + تست توکن سفارش
tests/test_wallet.py        فیکسچر بدون-اینترنت + تست توکن کیف پول
.env.example / README.md    مستندات PRICE_HISTORY_DAYS و API_TOKEN
```

### کامیت دوم (v2.1.0)
```
app/config.py          + TRADE_ALLOWED_DOMAINS
app/trade.py           + validate_trade_url() — allowlist در build_request (لایهی دوم)
app/main.py            + _check_trade_url() برای create/update کانکتور سفارش
frontend/src/lib/token.ts          (جدید) get/set توکن در localStorage
frontend/src/lib/api.ts            ارسال X-API-Token روی همهی درخواستها
frontend/src/App.tsx               فیلد «توکن امنیتی API» در صفحهی تنظیمات
public/                            بیلد جدید (index-BfCeLN1a.js)
tests/test_trade.py                + ۲ تست allowlist
.env.example / README.md           مستندات TRADE_ALLOWED_DOMAINS
app/version.py                     2.0.6 ← 2.1.0
```

**شروع تستها:** `python -m pytest tests/ -q` → `78 passed`

**فعالسازی روی سرور (در `.env`):**
```
API_TOKEN=secret      # 1) گیت توکن — تنظیمات UI هم آن را میفرستد
TRADE_ALLOWED_DOMAINS=api.wallex.ir,api.nobitex.ir   # 2) لیست سفید سفارش
```
